import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ej2.appcursos',
  appName: 'ej2-appcursos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
